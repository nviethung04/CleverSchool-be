services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Clever School-app
    ports:
      - "8080:8080"

  postgres:
    image: postgres:15-alpine
    container_name: Clever School-postgres
    environment:
      POSTGRES_DB: Clever School_db
      POSTGRES_USER: Clever School_user
      POSTGRES_PASSWORD: qlsJWiklVEKPCic0cpjOjMbJ
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - Clever School-network

  redis:
    image: redis:7-alpine
    container_name: Clever School-redis
    command: redis-server --requirepass 0lJc4VUTFmyV
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - Clever School-network

networks:
  Clever School-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
