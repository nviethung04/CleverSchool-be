# Migrations Legacy (Archive)

Thư mục này chứa **269+ migration cũ** đã được thay thế bởi baseline `../migrations/` (v1.0.0).

- **Không chạy** trên database mới.
- Chỉ dùng để tham khảo lịch sử đổi schema hoặc so sánh khi debug.
- Ngày archive: 2026-06-04.

Xem tài liệu hiện tại: `be/docs/database/`.
