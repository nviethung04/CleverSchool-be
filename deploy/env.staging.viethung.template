# CleverSchool STAGING — copy: cp env.staging.viethung.template .env
# Thư mục VPS: /opt/cleverschool-staging/deploy

STACK_NAME=csstaging
API_DOMAIN=https://api-staging.viethung.uk
FRONTEND_URL=https://viethung.uk
ALLOW_ORIGINS=https://viethung.uk,http://localhost:3000

POSTGRES_DB=lms_db_staging
POSTGRES_USER=lms_user
POSTGRES_PASSWORD=CsStg_vh2026_Pg_mN4pR9wQ2
REDIS_PASSWORD=CsStg_vh2026_Rd_hJ6kT3vX8
JWT_SECRET=CsStg_viethung_uk_JWT_2026_min32chars_bW7q

RUN_MIGRATIONS=true
ENABLE_SWAGGER=false
APP_DEBUG=false
REDIS_ENABLED=true
REDIS_DB=0

# Dashboard pre-cache + daily stats (không cần cho MVP — login, khóa học, bài tập)
ENABLE_DASHBOARD_JOBS=false

# S3: chỉ bật khi có bucket + key (upload/log). VPS không cấu hình → log chỉ stdout.
# AWS_BUCKET=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=
